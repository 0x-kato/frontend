To run the project, simple make sure that this is running on http://localhost:3000, make sure the backend is running on http://localhost:3333 and start the frontend with:

````bash
npm run start```
````
